[package]
name = "kenjutsu-desktop"
version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Kenjutsu desktop app — Tauri 2 GUI with GitHub PR integration"
edition = "2024"
default-run = "kenjutsu-desktop"

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "kenjutsu_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
git2 = { workspace = true }
kenjutsu-core = { workspace = true, features = ["specta", "highlighting"] }
kenjutsu-types = { workspace = true, features = ["specta", "serde"] }
log = { workspace = true }
comment-commit = { workspace = true, features = ["specta"] }
marker-commit = { workspace = true }
reqwest = { version = "0.12", features = ["json"] }
serde = { workspace = true }
serde_json = { workspace = true }
specta = { workspace = true }
specta-typescript = "0.0.9"
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
tauri-plugin-log = "2"
tauri-plugin-opener = "2"
tauri-plugin-store = "2"
tauri-specta = { version = "=2.0.0-rc.21", features = ["javascript", "typescript"] }
thiserror = { workspace = true }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }


[dev-dependencies]
test-repo = { workspace = true }
[target."cfg(any(target_os = \"macos\", windows, target_os = \"linux\"))".dependencies]
tauri-plugin-single-instance = "2.0.0"
